# num_match 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 在现有数字识别系统上新增游戏辅助脚本，截屏识别目标数字和 8x10 网格，用方向键移动光标到匹配位置。

**架构：** 扩展现有 find_num.py（新增 grid_recognize），新建 num_match.py 入口串联截屏→识别→搜索→方向键移动全流程，配置文件新增 targets 和 grid 区块。

**技术栈：** Python + mss (截屏) + PIL (图像) + pydirectinput (方向键) + find_num (数字识别，纯 Python 推理)

---

### 任务 1：扩展示例配置文件

**文件：**
- 修改：`num_config.json`

- [ ] **步骤 1：添加 targets 和 grid 配置块**

将 `num_config.json` 替换为以下内容。坐标需用户根据实际游戏截图填入：

```json
{
  "targets": {
    "t0": {"x1": 0, "y1": 0, "x2": 10, "y2": 10, "digits": 2},
    "t1": {"x1": 0, "y1": 0, "x2": 10, "y2": 10, "digits": 2}
  },
  "grid": {
    "x": 0, "y": 0,
    "cell_w": 10, "cell_h": 10,
    "cols": 10, "rows": 8,
    "cursor_w": 4
  }
}
```

- [ ] **步骤 2：验证 JSON 格式有效**

```powershell
python -c "import json; json.load(open('num_config.json')); print('OK')"
```

预期：`OK`

- [ ] **步骤 3：Commit**

```bash
git add num_config.json
git commit -m "feat: add targets and grid config blocks for num_match"
```

---

### 任务 2：在 find_num.py 中新增 grid_recognize()

**文件：**
- 修改：`find_num.py`（在文件末尾追加新函数）

- [ ] **步骤 1：追加 grid_recognize() 函数**

在 `find_num.py` 末尾追加：

```python
def grid_recognize(screenshot, grid_config):
    """识别网格区域中每个格子的两位数。

    Args:
        screenshot: PIL Image，全屏截屏
        grid_config: dict, 包含 x, y, cell_w, cell_h, cols, rows, cursor_w

    Returns:
        list[list[str]]，rows 行 cols 列的字符串矩阵，每个元素为两位数如 "42"
    """
    w = _get_weights()
    gx = grid_config["x"]
    gy = grid_config["y"]
    cw = grid_config["cell_w"]
    ch = grid_config["cell_h"]
    cols = grid_config["cols"]
    rows = grid_config["rows"]

    result = []
    for r in range(rows):
        row_data = []
        for c in range(cols):
            x1 = gx + c * cw
            y1 = gy + r * ch
            x2 = x1 + cw
            y2 = y1 + ch

            crop = screenshot.crop((x1, y1, x2, y2))

            if crop.size[0] < 2:
                row_data.append("")
                continue

            half = crop.size[0] // 2
            left = crop.crop((0, 0, half, crop.size[1]))
            right = crop.crop((half, 0, crop.size[0], crop.size[1]))

            lv = preprocess(left)
            rv = preprocess(right)
            l_idx = forward(lv, w["w1"], w["b1"], w["w2"], w["b2"])
            r_idx = forward(rv, w["w1"], w["b1"], w["w2"], w["b2"])
            row_data.append(f"{l_idx}{r_idx}")
        result.append(row_data)
    return result
```

- [ ] **步骤 2：运行现有测试确保未破坏**

```powershell
python test_find_num.py
```

预期：`All tests passed!`（14 项测试通过）

- [ ] **步骤 3：Commit**

```bash
git add find_num.py
git commit -m "feat: add grid_recognize() for batch grid digit recognition"
```

---

### 任务 3：创建 num_match.py 入口脚本

**文件：**
- 创建：`num_match.py`

- [ ] **步骤 1：编写完整入口脚本**

创建 `num_match.py`：

```python
import time
import mss
import pydirectinput
from PIL import Image
from find_num import load_config, recognize, grid_recognize

DELAY = 0.05


def capturar_tela():
    with mss.mss() as sct:
        monitor = sct.monitors[1]
        sct_img = sct.grab(monitor)
        return Image.frombytes("RGB", sct_img.size, sct_img.bgra, "raw", "BGRX")


def mover_cursor(dr, dc):
    if dr > 0:
        pydirectinput.press("down", presses=dr, interval=DELAY)
    elif dr < 0:
        pydirectinput.press("up", presses=-dr, interval=DELAY)

    if dc > 0:
        pydirectinput.press("right", presses=dc, interval=DELAY)
    elif dc < 0:
        pydirectinput.press("left", presses=-dc, interval=DELAY)


def main():
    START_ROW = 3
    START_COL = 3

    config = load_config("num_config.json")
    targets_cfg = config.get("targets", {})
    grid_cfg = config.get("grid", {})

    if not targets_cfg:
        print("[num_match] targets config not found")
        return
    if not grid_cfg:
        print("[num_match] grid config not found")
        return

    print("[num_match] capturando tela...")
    screenshot = capturar_tela()

    print("[num_match] reconhecendo alvos...")
    targets = recognize(screenshot, targets_cfg)
    t0 = targets.get("t0", "")
    t1 = targets.get("t1", "")
    if not t0 or not t1:
        print(f"[num_match] falha ao reconhecer alvos: t0={t0} t1={t1}")
        return
    print(f"[num_match] alvos: t0={t0} t1={t1}")

    print("[num_match] reconhecendo grade...")
    grade = grid_recognize(screenshot, grid_cfg)
    print("[num_match] grade reconhecida:")
    for row in grade:
        print("  " + " ".join(row))

    print("[num_match] buscando correspondencia...")
    rows = grid_cfg["rows"]
    cols = grid_cfg["cols"]
    encontrado = None
    for r in range(rows):
        for c in range(cols - 3):
            if grade[r][c] == t0 and grade[r][c + 1] == t1:
                encontrado = (r, c)
                break
        if encontrado:
            break

    if encontrado is None:
        print("[num_match] correspondencia nao encontrada")
        return

    r, c = encontrado
    print(f"[num_match] correspondencia encontrada: linha={r} coluna={c}")
    dr = r - START_ROW
    dc = c - START_COL
    print(f"[num_match] movendo: dr={dr} dc={dc}")
    mover_cursor(dr, dc)
    print("[num_match] concluido")


if __name__ == "__main__":
    main()
```

- [ ] **步骤 2：验证脚本语法正确**

```powershell
python -m py_compile num_match.py; if ($?) { Write-Output "syntax OK" }
```

预期：`syntax OK`

- [ ] **步骤 3：Commit**

```bash
git add num_match.py
git commit -m "feat: add num_match.py entry script for grid matching"
```

---

### 任务 4：端到端测试

**文件：**
- 无需创建/修改，使用已有测试框架

- [ ] **步骤 1：运行 test_find_num.py 确认全部通过**

```powershell
python test_find_num.py
```

预期：`All tests passed!`

- [ ] **步骤 2：合成场景手动验证 grid_recognize**

```powershell
python -c "
from PIL import Image
from find_num import _set_weights

mock_w = {
    'w1': [[0.01]*256 for _ in range(32)],
    'b1': [0.0]*32,
    'w2': [[0.0]*32 for _ in range(10)],
    'b2': [0.0]*10
}
for i in range(32):
    mock_w['w2'][5][i] = 100.0
_set_weights(mock_w)

screenshot = Image.new('RGB', (500, 300), color=(0, 0, 0))
for r in range(3):
    for c in range(5):
        x0 = 10 + c * 30
        y0 = 10 + r * 25
        for x in range(x0, x0 + 24):
            for y in range(y0, y0 + 20):
                screenshot.putpixel((x, y), (255, 255, 255))

from find_num import grid_recognize
grid_cfg = {'x': 10, 'y': 10, 'cell_w': 24, 'cell_h': 20, 'cols': 5, 'rows': 3, 'cursor_w': 4}
result = grid_recognize(screenshot, grid_cfg)
assert len(result) == 3 and len(result[0]) == 5, 'wrong dimensions'
assert all(v == '55' for row in result for v in row), f'unexpected values: {result}'
print('PASS grid_recognize smoke test')
"
```

预期：`PASS grid_recognize smoke test`

- [ ] **步骤 3：Commit**（如有任何代码调整）

```bash
git add -A
git commit -m "test: add grid_recognize smoke test"
```
